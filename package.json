{
  "name": "maker",
  "version": "1.0.0",
  "scripts": {
    "build": "pnpm -r --filter='./packages/*' run build",
    "client": "pnpm -r --filter='client' pnpm"
  },
  "keywords": [
    "uniswap v3",
    "liquidity",
    "maker"
  ]
}
